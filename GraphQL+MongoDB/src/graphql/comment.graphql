scalar DateTime

type Comment {
  id: String!
  name: String!
  content: String
  userId: String
  createdAt: DateTime
  updatedAt: DateTime
}

input CreateCommentInput {
  name: String!
  content: String
  userId: String
}

type Mutation {
  addComment(comment: CreateCommentInput): Comment
}
